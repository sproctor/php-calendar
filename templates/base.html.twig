<!doctype html>
<html lang="{{ app.request.locale }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{{ calendar.getTitle() }}</title>

    <link rel="icon" href="static/office-calendar.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="static/phpc.css">
    <script src="https://kit.fontawesome.com/7edf042137.js" crossorigin="anonymous"></script>
</head>
<body>
{% if calendar is defined %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        {% if calendars is defined and calendars|length > 1 %}
            <div class="nav-item dropdown">
                <a href="#" class="navbar-brand dropdown-toggle" id="phpcTitle" role="button" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">{{ calendar.getTitle }}</a>
                <div class="dropdown-menu" aria-labelledby="phpcTitle">
                    {% for cal in calendars %}
                        <a class="dropdown-item" href="{{ script }}?phpcid={{ cal.getCid }}">{{ cal.getTitle }}</a>
                    {% endfor %}
                </div>
            </div>
        {% endif %}
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#phpc-navbar"
                aria-controls="phpc-navbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="phpc-navbar">
            <ul class="navbar-nav mr-auto">
                {% if calendar.canWrite(user) %}
                    {{ menu_item("event_form", "create-event-menu-link"|trans, 'plus') }}
                {% endif %}
                {{ menu_item("display_month", "view-month-menu-link"|trans, 'eye') }}
                {% if action == "display_event" %}
                    {{ menu_item("display_day", "view-date-menu-link"|trans, 'eye') }}
                {% endif %}
                {% if user.isAdmin() or calendar.canAdmin(user) %}
                    {{ menu_item("admin", "admin-menu-link"|trans, 'cog') }}
                {% endif %}
                {% if user.isAnonymous() %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="langDropdown" role="button"
                           data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i
                                    class="fas fa-language"></i> {{ "language-menu-selector"|trans({'%locale%': locale}) }}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="langDropdown">
                            {% for lang,code in languages %}
                                <a class="dropdown-item"
                                   href="{{ append_parameter_url(app.request, "lang=" ~ code) }}">{{ lang }}</a>
                            {% endfor %}
                        </div>
                    </li>
                {% endif %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false"><i class="fas fa-user"></i> {{ user.getUsername() }}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="userDropdown">
                        <a class="dropdown-item" href="{{ context.createUrl("user_settings") }}"><i
                                    class="fas fa-cog"></i> {{ "settings-menu-link"|trans }}</a>
                        {% if not user.isAnonymous() %}
                            <a class="dropdown-item"
                               href="{{ context.createUrl("logout") }}&amp;lasturl={{ query_string|url_encode }}"><i
                                        class="fas fa-sign-out-alt"></i> {{ "log-out-menu-link"|trans }}</a>
                        {% else %}
                            <a class="dropdown-item"
                               href="{{ context.createUrl("login") }}&amp;lasturl={{ query_string|url_encode }}"><i
                                        class="fas fa-sign-in-alt"></i> {{ "log-in-menu-link"|trans }}</a>
                        {% endif %}
                    </div>
                </li>
            </ul>
            <form class="form-inline" action="{{ context.createUrl('search') }}" method="post">
                <div class="search-wrapper">
                    <label class="search-label" for="search-input"><i class="fas fa-search"></i></label>
                    <input id="search-input" class="search-input" name="query" type="search"
                           placeholder="{{ "search-label"|trans }}" aria-label="{{ "search-label"|trans }}">
                    <button class="search-button" title="{{ "search-label"|trans }}" type="submit">
                        <i class="fas fa-arrow-right"></i>
                    </button>
                </div>
            </form>
        </div>
    </nav>
{% endif %}

{% if messages is defined %}
    {% for message in messages %}
        <div class="alert alert-primary alert-dismissible fade show" role="alert">
            {{ message }}
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    {% endfor %}
{% endif %}

<div class="phpc-body container-fluid">
    {% block body %}{% endblock %}
</div>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
        crossorigin="anonymous"></script>
<script defer src="https://use.fontawesome.com/releases/v5.0.4/js/all.js"></script>
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
<!-- <script src="static/phpc.js"></script> -->

{#
{% if embed %}
    <script src="//cdnjs.cloudflare.com/ajax/libs/underscore.js/1.5.2/underscore-min.js"></script>
    <script src="static/embed.js"></script>
{% endif %}
#}
</body>
</html>
